<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wyy.demoshiro.mapper.SysUserRoleMapper">

    <insert id="insert">
        INSERT INTO sys_user_role (user_id, role_id)
        VALUES (#{userId}, #{roleId})
    </insert>

    <delete id="delete">
        DELETE FROM sys_user_role WHERE user_id = #{userId} AND role_id = #{roleId}
    </delete>

    <select id="findByUserId" resultType="com.wyy.demoshiro.entity.SysUserRole">
        SELECT * FROM sys_user_role WHERE user_id = #{userId}
    </select>

    <select id="findByRoleId" resultType="com.wyy.demoshiro.entity.SysUserRole">
        SELECT * FROM sys_user_role WHERE role_id = #{roleId}
    </select>

</mapper>
