<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wyy.demoshiro.mapper.SysRoleMapper">

    <insert id="insert">
        INSERT INTO sys_role (role_name, role_description, created_time, updated_time)
        VALUES (#{roleName}, #{roleDescription}, #{createdTime}, #{updatedTime})
    </insert>

    <select id="findById" resultType="com.wyy.demoshiro.entity.SysRole">
        SELECT * FROM sys_role WHERE id = #{id}
    </select>

    <select id="findAll" resultType="com.wyy.demoshiro.entity.SysRole">
        SELECT * FROM sys_role
    </select>

    <update id="update">
        UPDATE sys_role
        SET role_name = #{roleName}, role_description = #{roleDescription}, updated_time = #{updatedTime}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM sys_role WHERE id = #{id}
    </delete>

</mapper>
