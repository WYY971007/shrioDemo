<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wyy.demoshiro.mapper.SysPermissionMapper">

    <insert id="insert">
        INSERT INTO sys_permission (permission_name, permission_description, permission_type, created_time, updated_time)
        VALUES (#{permissionName}, #{permissionDescription}, #{permissionType}, #{createdTime}, #{updatedTime})
    </insert>

    <select id="findById" resultType="com.wyy.demoshiro.entity.SysPermission">
        SELECT * FROM sys_permission WHERE id = #{id}
    </select>

    <select id="findAll" resultType="com.wyy.demoshiro.entity.SysPermission">
        SELECT * FROM sys_permission
    </select>

    <update id="update">
        UPDATE sys_permission
        SET permission_name = #{permissionName}, permission_description = #{permissionDescription},
        permission_type = #{permissionType}, updated_time = #{updatedTime}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM sys_permission WHERE id = #{id}
    </delete>

</mapper>
